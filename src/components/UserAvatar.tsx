// src/components/UserAvatar.tsx
import React from 'react';
import withThemeProvider from '../themes/withThemeProvider';

/**
 * UserAvatar component displays a user's avatar.
 * It can be customized with theming and other props.
 *
 * @returns {React.ReactElement} The UserAvatar component.
 */
const UserAvatar: React.FC = () => {
  // Component logic
  return <div>User Avatar</div>;
};

export default withThemeProvider(UserAvatar);
